__pycache__/
*.py[cod]
*$py.class
*.so
.env
venv/
env/
*.log
*.db
.DS_Store
# Ignore the real settings file containing passwords
config/settings.yaml
# Keep the template if we create one
!config/settings.template.yaml

# Ignore data directory (contains local state and user settings with secrets)
data/
